# signal-cli-jsonrpc

Strongly typed Python dataclass interface to signal-cli's RPC commands.

## Versioning

Published versions follow the version number of signal-cli upstream, followed by `.dev` and a local version number.
